# TTS Playground

Веб-приложение для синтеза речи на русском языке с использованием современных нейросетевых моделей. Проект предоставляет удобный интерфейс для генерации высококачественной речи с учетом ударений, пауз и знаков препинания.

## 🎯 Основные возможности

- **Высококачественный синтез речи** на русском языке с использованием модели Silero TTS v5
- **Автоматическая расстановка ударений** с помощью библиотеки RUAccent для более естественного произношения
- **Выбор голоса** из 6 доступных вариантов (мужские и женские голоса)
- **Учет пауз и знаков препинания** для естественной интонации
- **Веб-интерфейс** на базе Streamlit для удобной работы
- **Измерение производительности** с отображением времени генерации

## 🚀 Быстрый старт

### Требования

- Python 3.8 или выше
- PyTorch (устанавливается автоматически)
- Подключение к интернету для загрузки моделей (только при первом запуске)
- ~500 МБ свободного места на диске для моделей

### Установка

1. **Клонируйте репозиторий или скачайте проект:**
   ```bash
   git clone <repository-url>
   cd TTSplayground
   ```

2. **Создайте виртуальное окружение (рекомендуется):**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   
   **Windows:**
   ```bash
   venv\Scripts\activate
   ```
   
   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Установите PyTorch (если не установлен):**
   
   Для CPU:
   ```bash
   pip install torch torchaudio
   ```
   
   Для CUDA 13:
   ```bash
   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu130
   ```
   
   Для других версий CUDA см. [официальный сайт PyTorch](https://pytorch.org/get-started/locally/).

### Запуск

```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## 📖 Использование

### Основной интерфейс

1. **Введите текст** в текстовое поле для озвучки
2. **Выберите настройки** в боковой панели:
   - Включите/выключите обработку ударений
   - Выберите голос для синтеза
3. **Нажмите кнопку "🎤 Сгенерировать голос"**
4. **Прослушайте результат** и посмотрите время генерации

### Доступные голоса

- **Айдар** (aidar) - мужской голос
- **Бая** (baya) - женский голос
- **Ксения** (kseniya/xenia) - женский голос (по умолчанию)
- **Евгений** (eugene) - мужской голос
- **Случайный** (random) - случайный выбор голоса

### Обработка ударений

При включенной опции "Использовать обработку ударений":
- Текст автоматически обрабатывается библиотекой RUAccent
- Ударения расставляются для более правильного произношения
- Обработанный текст с ударениями отображается перед генерацией
- Модель Silero TTS учитывает расставленные ударения при синтезе

## 🛠 Технические детали

### Используемые технологии

- **Streamlit** - веб-фреймворк для создания интерфейса
- **PyTorch** - фреймворк для машинного обучения
- **Silero TTS v5** - модель синтеза речи для русского языка
- **RUAccent** - библиотека для автоматической расстановки ударений
- **NumPy** - работа с массивами для обработки аудио

### Архитектура

```
TTS Playground
├── app.py                 # Основное приложение
├── requirements.txt       # Зависимости проекта
├── README.md             # Документация
└── venv/                 # Виртуальное окружение (не в git)
```

### Производительность

- **Время генерации:** обычно < 1 секунды для коротких текстов
- **Качество:** высокое качество синтеза с естественной интонацией
- **Ресурсы:** работает на CPU, GPU не требуется (но поддерживается)

## 📦 Зависимости

Основные зависимости указаны в `requirements.txt`:

- `streamlit>=1.29.0` - веб-фреймворк
- `torch>=2.1.0` - PyTorch (устанавливается отдельно)
- `torchaudio>=2.1.0` - обработка аудио
- `transformers>=4.38.0` - библиотека трансформеров
- `ruaccent>=1.0.0` - расстановка ударений
- `librosa>=0.10.0` - обработка аудио
- `soundfile>=0.12.0` - работа с аудиофайлами
- `numpy>=1.24.0` - численные вычисления
- `omegaconf>=2.3.0` - конфигурация


## 🎨 Особенности интерфейса

- **Интуитивный веб-интерфейс** с боковой панелью настроек
- **Визуальная обратная связь** при обработке текста и генерации
- **Отображение обработанного текста** с ударениями
- **Плеер аудио** для прослушивания результата
- **Индикатор производительности** с временем генерации

## 📝 Примеры использования

### Базовый пример

```python
# Введите текст в интерфейсе:
"Привет! Как слышно?"

# Выберите голос: Ксения
# Включите обработку ударений
# Нажмите "Сгенерировать голос"
```

### Текст с ударениями

При включенной обработке ударений текст автоматически обрабатывается:
- Входной текст: "замок на двери"
- Обработанный текст: "зам+ок на двери" (ударение на первом слоге)

## 🔒 Безопасность

- Модели загружаются из официальных источников (GitHub, Hugging Face)
- Все вычисления выполняются локально
- Данные не передаются на внешние серверы
- Виртуальное окружение изолирует зависимости проекта

## 📄 Лицензия

См. файл `LICENSE` для подробной информации о лицензии.


## 📚 Дополнительные ресурсы

- [Silero TTS GitHub](https://github.com/snakers4/silero-models) - официальный репозиторий Silero TTS
- [RUAccent GitHub](https://github.com/Den4ikAI/ruaccent) - библиотека для расстановки ударений
- [Streamlit Documentation](https://docs.streamlit.io/) - документация Streamlit
- [PyTorch Documentation](https://pytorch.org/docs/) - документация PyTorch

## 🐛 Известные проблемы

- При первом запуске требуется подключение к интернету для загрузки моделей
- Модели занимают значительное место на диске (~500 МБ)
- Длинные тексты могут генерироваться медленнее


